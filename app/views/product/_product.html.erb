<div class="list">
  <tr>
    <td><h1><%= link_to product.product_name, product_path(product) %></h1></td>
    <td><%= link_to image_tag(product.image), product_path(product) %></td>
    <td><p><%= product.stock_quantity %> left</p></td>
    <td><p>Price: $<%= product.price %></p></td>
  </tr>
</div>
